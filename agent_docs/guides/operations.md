# Операции: запуск, деплой, прод

**Назначение:** всё, что нельзя вывести из кода — как запустить, как задеплоить, где живут секреты,
как посмотреть прод и на какие инфраструктурные грабли уже наступили. Третья ось памяти проекта
(рядом с контрактами и state, см. `AGENTS.md` → «Память проекта»).

**Когда заполнять:** при первом запуске проекта локально, при первом деплое и **каждый раз, когда
разгадывание инфраструктуры заняло больше пары минут** — иначе следующая сессия разгадает это заново.

> Заглушки ниже — удалить или заполнить. Пустой раздел лучше удалить, чем оставить `<!-- TODO -->`
> навсегда: незаполненный шаблон читается как «здесь ничего нет», а не «здесь не разобрались».

---

## Запуск локально

- Как поднять: <!-- TODO: команда(ы), порт, что должно появиться в выводе -->
- Зависимости/окружение: <!-- TODO: интерпретатор/рантайм и КОНКРЕТНАЯ команда (python3 vs python, pnpm vs npm), где живут пакеты -->
- Тесты: <!-- TODO: команда прогона; сколько тестов зелёных сейчас; что нужно доустановить, иначе часть падает на импорте -->

## Деплой

> Стандарт пайплайна и его включение — `agent_docs/guides/autodeploy.md`. Здесь — конкретика
> этого проекта: имена, адреса, отличия от стандарта.

- Триггер: <!-- TODO: что именно вызывает деплой (push в main? тег? ручной шаг?) -->
- Пайплайн: <!-- TODO: шаги и гейты — что не пропустит красные тесты/линт -->
- Кто применяет миграции/сборку: <!-- TODO: CI, контейнер на старте, вручную? -->
- Как проверить, что доехало: <!-- TODO: команда/URL проверки версии и здоровья -->
- Как откатить: <!-- TODO: новым коммитом? предыдущий образ? снапшот БД? -->

## Секреты и переменные окружения

- Где хранятся: <!-- TODO: .env локально / секреты CI / панель хостинга / секрет-менеджер -->
- Что нужно, чтобы добавить новую переменную: <!-- TODO: полный список мест, где её надо прописать -->
- Что НЕ задавать вручную: <!-- TODO: производные значения, которые собираются автоматически -->

Секреты в этот файл не копировать (см. INV-001) — только имена и место хранения.

## Доступ к прод-окружению

- Как подключиться: <!-- TODO: хост/бастион/панель -->
- Типовые проверки: <!-- TODO: логи, статус процессов, версия схемы, health -->
- Правила безопасности при работе с продом: <!-- TODO: таймауты на удалённые команды, что нельзя запускать в фоне, что требует согласования владельца -->

**Запись в прод-данные — необратимое действие: согласовывать с владельцем.** Диагностический
вопрос («почему пусто?») не является разрешением что-то засеять. Для проверки логики без следов —
транзакция с откатом в конце.

## Журнал инфраструктурных грабель

Каждая запись — симптом, причина, правило на будущее. Ценность в том, что симптом узнаваем:
следующая сессия ищет по симптому, а не переоткрывает причину.

| Симптом (что видно) | Причина | Правило на будущее |
|---|---|---|
| <!-- TODO: удалить строку-пример --> | | |

Пример заполнения:

| Симптом | Причина | Правило |
|---|---|---|
| Кнопка «редеплой» в панели падает с `pull access denied` | Панель ПУЛЛИТ образ из реестра, а наши образы собираются локально и в реестр не пушатся | Деплоить только через webhook (он собирает); новому сервису не давать свой `build:` — переиспользовать уже собираемый образ |
| Rate-limit по IP не срабатывает; в логах/аудите чужие адреса | Прокси ДОПИСЫВАЕТ клиентский `X-Forwarded-For` к реальному адресу, а приложение берёт левый элемент — его задаёт клиент | Брать правый элемент XFF (INV-004). Правку на прокси проверять на живучесть: если она внутри образа, а не в томе — вернётся при обновлении |
| Правка заголовков прокси «применилась, но не действует» | В nginx `proxy_set_header` наследуется с уровня выше ТОЛЬКО если на текущем уровне нет своих; панели пишут такие правки на уровень `server`, а `location` определяет свои | Менять общий инклюд, который подключается внутри `location`, либо задавать заголовки в самой `location` |
| Новый секрет добавлен в панель, но внутри контейнера пусто | Значение подставляется только там, где переменная явно перечислена в compose | Добавляя секрет — прописать его строкой в нужном сервисе, потом проверить `printenv` внутри контейнера |
