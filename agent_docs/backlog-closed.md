# Backlog — закрытое

Архив закрытых пунктов `backlog.md`: пункт переезжает сюда с галочкой и строкой
«Закрыт <дата>: <чем доказано>», текст — как был. При старте сессии не читать; когда нужен
контекст по номеру — искать `grep`'ом.

## Закрыто
