# Чек-лист инициализации нового проекта

Одноразовый чек-лист для нового репозитория из шаблона. После прохождения — удалить файл.

## 1. Описание проекта

Заполнить раздел «Описание проекта» в `AGENTS.md`:

- Название и назначение
- Цели пользователя/команды
- Сценарии использования и аудитория
- Контекст и ограничения

## 2. Окружение

Применить `agent_docs/guides/environment-setup.md`:

- `.vscode/settings.json` — создать/обновить видимость `.env` в IDE
- `.cursorignore` — проверить/обновить правила скрытия секретов от AI-агентов
- `.gitignore` — проверить правила `.env`/`.env.*`
- `.env.example` — проверить/обновить шаблон переменных

## 2a. Операции (заполнить при первом запуске и первом деплое)

Заполнить `agent_docs/guides/operations.md`: команда запуска, команда тестов, триггер и шаги деплоя,
где живут секреты, как посмотреть прод. Незаполненные разделы — удалить, не оставлять `TODO` навсегда.

Смысл: это знание нельзя вывести из кода, и если его не записать — каждая новая сессия
разгадывает инфраструктуру заново (или оно утекает в приватную память агента, невидимую другим).

## 3. Глоссарий

Заполнить `agent_docs/glossary.md` начальной терминологией: специфичные термины, аббревиатуры, кодовые названия, ссылки на внешние ресурсы.

## 4. Архитектура (если применимо)

Заполнить `agent_docs/architecture.md` или удалить, если структура проекта тривиальна и не требует описания.

## 4a. Инварианты

Просмотреть `agent_docs/invariants.md`: стартовые инварианты (INV-001…003) даны как примеры — подтвердить их применимость и дополнить своими.

## 5. Project-local skills (если нужны)

- Создать canonical source: `.agents/skills/<name>/SKILL.md`.
- Создать platform mirrors:
  - `.claude/skills/<name> -> ../../.agents/skills/<name>`
  - `.codex/skills/<name> -> ../../.agents/skills/<name>`
  - `.cursor/skills/<name> -> ../../.agents/skills/<name>`
- Не создавать slash-command файлы; workflow оформлять как skills.

## 6. Очистить шаблон

- Удалить этот файл (`agent_docs/setup-checklist.md`).
- Обновить `README.md` под содержание своего проекта.
