# Backlog

Что планируется, но ещё не делается. Граница с другими файлами:

- `snapshot.md` — что **в работе прямо сейчас**.
- `backlog.md` (этот файл) — что **будет потом**, но уже признано как работа.
- `backlog-closed.md` — что **закрыто**, с доказательством; при старте сессии не читать.

**Правила файла.** Пункт — одна-три строки: `- [ ] <id>: **<заголовок>.** <контекст> — <ссылка
на запись журнала или ТЗ>`. Id — сквозной (B-001, B-002…); следующий свободный держать здесь:
**занято по B-000**. Закрытие пункта — галочка, строка «Закрыт <дата>: <чем доказано>» и переезд
в `backlog-closed.md` тем же коммитом; «сделано» внутри открытого пункта не писать — галочка
обязана быть правдой. Историю решений по пункту не копить в его теле: она в `adr/` и в журнале,
здесь — ссылка. Потолок — 300 строк, CI проверяет. Дубли номеров:
`grep -ohE '^- \[.\] B-[0-9]+[a-z]?' agent_docs/backlog*.md | sort | uniq -d`.

## Next

<!-- Возьмётся следующим, по приоритету сверху вниз. Пункт должен быть достаточно конкретным, чтобы начать без research. -->

## Soon

<!-- Известная работа на ближайшее время, ещё не детализирована. -->

## Later

<!-- Признано нужным, но не запланировано. -->

## Won't do

<!-- Явно решённые «нет», один пункт = одна причина, без галочек: это решения, а не задачи. Чтобы не открывать закрытые дебаты заново. -->
